/**
 * `ui` — Compose screens and view models. Per `00-system-architecture.md`'s boundary rule,
 * code in this package depends only on `domain`, never directly on `data`.
 *
 * Stub-empty: populated starting with the `20-map-view` Job.
 */
package com.two49gmap.app.ui
