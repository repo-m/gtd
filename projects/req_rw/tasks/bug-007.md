# Task

Add the missing trailing period to the empty-document message in `TableView.tsx` so it matches the exact string quoted in the empty-states spec.

## Specs to load

- `specs/62-empty-states.md` — §"Empty document": the exact message string

## Acceptance criteria

1. The empty-document `<td>` in `TableView.tsx` renders the text: `No requirements yet — double-click here or use Add Req to get started.` (with trailing period).
2. No other text or markup in the file is changed.
3. `npx tsc --noEmit` exits 0.

## Done when

The rendered string matches the spec verbatim.

## Gate command

```
npx tsc --noEmit
```

## Out of scope

- Any other part of `TableView.tsx`.
- Empty-state styling, layout, or behaviour.
